/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PiecePipe } from './piece.pipe';

describe('Pipe: Piecee', () => {
  it('create an instance', () => {
    let pipe = new PiecePipe();
    expect(pipe).toBeTruthy();
  });
});
